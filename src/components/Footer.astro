<style>
.div {
  background-image: linear-gradient(90deg, #7e0000, #a40000, #7e0000); /* 更接近血液的暗红色渐变 */
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
  animation: blood-flow 2.5s infinite linear; /* 加快流动动画速度 */
  filter: blur(0.7px); /* 轻微模糊，增强动态感 */
  font-size: 1.5rem; /* 缩小字体 */
  font-weight: bold; /* 加粗字体 */
  text-align: center; /* 居中对齐 */
  line-height: 1.5; /* 增加行高，提升可读性 */
}

@keyframes blood-flow {
  0% {
    background-position: 0% 50%;
  }
  100% {
    background-position: 200% 50%; /* 增强流动感 */
  }
}

#meng a {
  text-decoration: none; /* 去掉下划线 */
  color: inherit; /* 保持渐变色 */
}

/* 将 footer 固定在页面底部 */
footer {
  position: fixed;
  bottom: 0;
  left: 0;
  width: 100%; /* 占满宽度 */
  background: rgba(0, 0, 0, 0.5); /* 添加一个半透明背景，提升可读性 */
  padding: 10px 0; /* 增加上下内边距 */
  z-index: 1000; /* 确保在最上层 */
}
</style>

<footer>
  <div id="meng" class="div">
    <a href="#">感觉我一直活在梦里，可能我死了又可能我真的在做梦</a>
  </div>
</footer>
